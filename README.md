# substrate-kitties-frontend
* Go into the `<project_name>-ui` folder and run:
    * `yarn install`
    * `yarn start
    * This should start a web server on `localhost:8000` where you can interact with your node
